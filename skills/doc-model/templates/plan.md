# <slug> — <проверяемый outcome одной строкой>

> Канон статуса: <файл из routing-таблицы L0> → строка `<slug>`. План — рабочая история и текущий шаг, не источник статуса.

- **Обновлено:** YYYY-MM-DD
- **Next action:** один шаг, владелец, дата, ожидаемое доказательство — обновляется при каждом write-back
- **Scope / claim:** `slug:<slug>`
- **DoD:** проверяемый результат, кому подан

## Текущее состояние

Что доказано, что нет; команды проверки и их результат без секретов.

## Лог (append-only, новое снизу)

- YYYY-MM-DD — что сделано, что проверено, что решено.
